# Coding style

- Save source code in `src`.
- Keep secrets out of git & GitHub
- Copy .env.example into .env before modifying
- Save dependencies using `pip freeze > requirements.txt` for reuse
- Write modular scalable code